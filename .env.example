# Email Configuration for Password Reset
# Copy this file to .env and fill in your credentials

# 1. For CONSOLE BACKEND (Default - emails print to console):
# Leave EMAIL_BACKEND commented or set to console

# 2. For GMAIL SMTP:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=your-email@gmail.com

# NOTE: For Gmail, you MUST use an "App Password", NOT your regular Gmail password:
# 1. Go to myaccount.google.com/apppasswords
# 2. Select Mail and Windows Computer (or your device)
# 3. Copy the 16-character password generated
# 4. Paste it as EMAIL_HOST_PASSWORD above

# 3. For Other SMTP Providers (e.g., SendGrid, AWS SES):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=your-smtp-host
# EMAIL_PORT=587 (or 25, 465 depending on provider)
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-username
# EMAIL_HOST_PASSWORD=your-password
# DEFAULT_FROM_EMAIL=sender@example.com
